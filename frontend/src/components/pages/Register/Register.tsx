import React from 'react';

const Register: React.FC = () => {
    return (
        <div>
            <h1>Cadastro</h1>
            {/* Formulário de cadastro aqui */}
        </div>
    );
};

export default Register;
