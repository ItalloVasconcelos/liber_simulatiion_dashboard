import React from 'react';

const Login: React.FC = () => {
    return (
        <div>
            <h1>Login</h1>
            {/* Formulário de login aqui */}
        </div>
    );
};

export default Login;
