import React from 'react';

const Dashboard: React.FC = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            {/* Formulário de cadastro aqui */}
        </div>
    );
};

export default Dashboard;
